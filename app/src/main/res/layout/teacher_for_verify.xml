<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/layout_list_bg"
    tools:context=".Teacher_For_Verify">

    <Button
        android:onClick="VerifyTeacher"
        android:id="@+id/verifyTeacher"
        android:text="@string/verify"
        android:textColor="#ffffff"
        android:textSize="18sp"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:layout_marginLeft="50dp"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        android:layout_alignParentBottom="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/NiceButton"/>

    <Button
        android:onClick="Delete"
        android:id="@+id/delete"
        android:text="@string/delete"
        android:textColor="#ffffff"
        android:textSize="18sp"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:layout_alignParentBottom="true"
        android:layout_toRightOf="@id/verifyTeacher"
        android:layout_marginLeft="50dp"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/NiceButton"/>

    <ListView
        android:id="@+id/listView1"
        android:background="@color/white"
        android:layout_above="@id/verifyTeacher"
        android:layout_alignParentTop="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</RelativeLayout>
